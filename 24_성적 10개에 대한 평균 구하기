#include <stdio.h>

int main()
{
  int score[10];
  int i, sum=0;
  for (i=0; i<=9; i++) {
    printf("성적: ");
    scanf("%d", &score[i]);
    sum = sum+score[i];
  }
  for (i=0; i<=9; i++) {
    printf("%d ", score[i]);
  }
  printf("\n평균: %f ", sum/10.0);
  return 0;
}
