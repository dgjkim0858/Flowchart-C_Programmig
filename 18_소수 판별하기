#include <stdio.h>

int main()
{
  int n, i, cnt=0;
  printf("수: ");
  scanf("%d", &n);
  for (i=1; i<=n; i++)
    if (n%i==0)
      cnt++;
  if (cnt==2)
    printf("소수임");
  else
    printf("소수아님");
  return 0;
}
